#include<stdio.h>
#include<stdlib.h>
int main()
{
    float x,y,res;
    printf("Enter 2 real values\n");
    scanf("%f",&x);
    scanf("%f",&y);
    if (x*y>0)
    {
        res=x+y;
    }
    else
    {
        res=x-y;
    }
    printf("the result is %f\n",res);
    return 0;
}
