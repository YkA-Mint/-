#include<stdio.h>
#define pi 3.1415926535
#include<math.h>
int main()
{
    double x,y;
    printf("Entrer un angle:");
    scanf("%lf",&x);
    y=cos(x*pi/180);
    printf("The cosine of %lf is:%lf",x,y);
    return 0;
}
